# FastAPI CI/CD Project
Проект с автоматической сборкой и базой данных.

### Как запустить:
1. Установите Docker и Docker Compose.
2. Выполните команду: `docker-compose up --build`
3. API будет доступно по адресу: http://localhost:8080